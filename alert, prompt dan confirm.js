// alert, prompt, dan confirm

// alert digunakan untuk memunculkan atau memberik peringatan berupa ppopup text di browser

// prompt digunakan untuk mengisi nilai string dengan input text
const name = confirm("henloo");

if (name) {
  const masuk = prompt("masukkan nama");
} else {
  alert("byeee");
}

// confirm merupakan nilai untukj Boolean,  true atau false , YA atau TIDAK
